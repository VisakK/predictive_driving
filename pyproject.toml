[project]
name = "driving"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "gymnasium",
  "numpy",
  "pyyaml",
  "tensorboard",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]